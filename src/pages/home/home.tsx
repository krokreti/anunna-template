import { Typography, Box, Grid } from "@mui/material";

const Home = () => {
    return (<Box border={'1px solid red'} height={'calc(100vh - 4em)'}>
        <Typography variant="h2">body</Typography>
    </Box>)
}

export default Home;